export default function CollectionPage() {
  return (
    <div className="wrap">
      <h1 className="display" style={{ fontSize: 24, marginBottom: 8 }}>Collection</h1>
      <p style={{ color: "var(--t2)" }}>
        Your inventory, portfolios, and value-over-time arrive in Phase 3 (ownership). For now,
        explore the catalog from <a href="/browse" style={{ color: "var(--br)" }}>Browse</a>.
      </p>
    </div>
  );
}
