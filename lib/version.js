// Single source of the app version. Bump this every release (with a CHANGELOG
// entry). The footer + /api/version read this; compare live vs built to detect
// a silently-stuck Vercel deploy.
export const APP_VERSION = "0.3.0";
